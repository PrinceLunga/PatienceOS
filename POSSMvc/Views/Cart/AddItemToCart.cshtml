@model POSSMvc.Models.Product

@{
    ViewData["Title"] = "AddItemToCart";
}


@*@if (Model != null)
{*@
    <div class="row">
        <div class="col-md-4">
            <form asp-action="AddItemToCart" method="post" enctype="multipart/form-data">

                <div class="text-center panel-body">
                    <button type="submit" class="btn btn-sm btn-primary">Add to Cart</button>
                </div>
                <thead><tr><th>Item Details</th><th></th></tr></thead>
                <tbody>
                    <td>
                        <div class="form-group">

                            <input asp-for="Name" class="form-control" />
                            <br />
                            <input asp-for="Group" class="form-control" />
                            <br />
                            <input asp-for="Status" class="form-control" />
                            <br />
                            <input asp-for="Price" class="form-control" />
                            <br />
                            <input asp-for="Quantity" class="form-control" />
                        </div>
                    </td>
                    <input asp-for="Image">
                    @{
                        var base64 = Convert.ToBase64String(Model.Image);
                        var imgsrc = String.Format("data:image/gif;base64,{0}", base64);
                    }
                    <img src="@imgsrc" width="400" height="300" />
                    </input>
                    </tr>
                </tbody>
                </table>
                @*}*@
            </form>
</div>
</div>
<div>
    @Html.ActionLink("Edit", "Edit", new { /* id = Model.PrimaryKey */ }) |
    <a asp-action="Index">Back to List</a>
</div>
